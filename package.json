{
  "name": "opsbrain",
  "version": "3.0.1",
  "private": true,
  "description": "OpsBrain — AI 网络运维平台",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,green \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd web/frontend && npm run dev",
    "dev:backend": "node scripts/run-backend.js --reload",
    "build": "cd web/frontend && npm run build",
    "start": "concurrently -n frontend,backend -c cyan,green \"npm run start:frontend\" \"npm run start:backend\"",
    "start:frontend": "cd web/frontend && npm run preview",
    "start:backend": "node scripts/run-backend.js",
    "install:all": "npm install && cd web/frontend && npm install",
    "install:python": "node scripts/install-python.js",
    "setup": "npm run install:all && npm run install:python",
    "init": "node scripts/init.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
